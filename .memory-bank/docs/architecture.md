---
file: .memory-bank/docs/architecture.md
description: "L1: системная архитектура — контейнеры, технологии, потоки данных"
version: '0.1.0'
date: '2026-09-11'
status: 'DRAFT'
c4_level: 'L1'
parent: '.memory-bank/index.md'
---

# System Architecture

> Заполняется при инициализации или первым архитектурным протоколом.
> Уровень C4-Container: из чего состоит система и как контейнеры общаются.

## Контейнеры

| Контейнер | Технологии | Ответственность | Документация |
|---|---|---|---|
| {api} | {NestJS/FastAPI/...} | {REST, авторизация} | docs/api/ (создать) |
| {client} | {React/Next/...} | {UI} | docs/client/ (создать) |
| {worker} | {...} | {фоновые задачи} | docs/worker/ (создать) |

## Потоки данных (главные)

1. {Пользователь → client → api → БД: ...}
2. {Асинхронные процессы: очередь → worker → ...}

## Хранилища и инфраструктура

- БД: {...}; кэш/очереди: {...}; файлы: {...}; деплой: {...}.

## Ключевые архитектурные решения (ADR-индекс)

> Крупные решения фиксируются в планах протоколов (ADR-резюме) и дублируются сюда одной строкой со ссылкой на протокол.

- {NNNN: принято решение X, потому что Y — см. .protocols/NNNN/plan.md}
